# pingboard — SPEC

> 미니 게시판 위에 얹은 **실시간 알림함**. 게시판/댓글은 알림을 발생시키기 위한 최소한의 그릇이고,
> **WebSocket 실시간 알림 전달의 신뢰성**이 이 프로젝트의 유일한 어필 포인트다.

| 항목      | 값                                                                                 |
| --------- | ---------------------------------------------------------------------------------- |
| 난이도    | **S** (하루 완성)                                                                  |
| 유형 태그 | **실시간(WebSocket)** — 워크스페이스 최초                                          |
| 백엔드    | NestJS 11 + **TypeORM 0.3** + Neon Postgres                                        |
| 프론트    | Vite + React + TS + Tailwind v4 + Zustand + TanStack Query + socket.io-client      |
| 실시간    | `@nestjs/websockets` + `@nestjs/platform-socket.io` (기본 socket.io 어댑터 그대로) |
| 배포      | 프론트 Vercel(확정) / 백엔드 로컬 Docker 기본, **Render 라이브는 승인 후 결정**    |

---

## 1. 문제 정의

### 1-1. 서비스가 푸는 문제

게시판에 댓글이 달렸는데 사용자가 그걸 알 방법이 없다. 새로고침해야만 안다.
pingboard는 **내 글에 댓글이 달리는 순간** 화면 갱신 없이 알림 뱃지가 올라가고, 알림함에서 바로 해당 글로 이동할 수 있게 한다.

### 1-2. 포트폴리오로서 푸는 문제 (진짜 목적)

리서치(`_portfolio-index/websocket-payment-ideas-research.md`) 결과, 신입 포트폴리오의 WebSocket 구현은
대부분 **"소켓 emit이 성공한다"까지만** 만들고 끝난다. 그래서 다음 두 가지가 공통적으로 비어 있다.

| 흔한 공백                      | 실제로 벌어지는 일                                                                            | pingboard가 하는 것                                                       |
| ------------------------------ | --------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- |
| **재연결 구간의 알림 유실**    | 지하철·와이파이 전환·서버 콜드스타트로 소켓이 끊긴 5초 동안 온 알림은 영원히 화면에 안 나타남 | 재연결 시 REST로 미읽음 전체 재동기화 (DB가 유일한 진실)                  |
| **다중 탭/기기에서 알림 누락** | `Map<userId, socket>` 로 소켓 1개만 저장 → 마지막에 연 탭에만 알림이 감                       | userId 기반 socket.io room으로 N개 연결에 동시 브로드캐스트 + 읽음 동기화 |

**즉 이 프로젝트의 주장은 "소켓을 붙일 줄 안다"가 아니라 "소켓은 신뢰할 수 없는 채널이라는 전제 위에 설계했다"이다.**
면접 스토리도 이 두 개로 간다.

---

## 2. 핵심 기능 범위

### 2-1. 포함 (이게 전부다)

| #   | 기능            | 상세                                                                                 |
| --- | --------------- | ------------------------------------------------------------------------------------ |
| F1  | 인증            | 회원가입 / 로그인(Passport-JWT, access token 단일) / 내 정보                         |
| F2  | 데모 계정       | `admin` / `admin` 원클릭 로그인 + 샘플 게시글·댓글·알림 시드                         |
| F3  | 게시글          | 목록(페이지네이션·댓글수) / 상세(댓글 포함) / **작성**                               |
| F4  | 댓글            | 게시글 상세에서 **작성**만                                                           |
| F5  | 알림 생성       | 남의 글에 댓글 작성 시 글쓴이에게 `COMMENT` 알림 1건 (댓글 저장과 **같은 트랜잭션**) |
| F6  | 실시간 수신     | 접속 중이면 소켓으로 즉시 `notification:new` → 뱃지 +1, 드롭다운 상단 삽입           |
| F7  | 오프라인/재연결 | 미접속 중 발생분은 로그인·재연결 시 **REST 재동기화**로 채워짐                       |
| F8  | 읽음 처리       | 개별 읽음 / 전체 읽음 → **다른 탭에도 실시간 반영**(`notification:read`)             |
| F9  | 소켓 인증       | `handshake.auth.token` JWT 검증, 실패 시 연결 거부(재시도 루프 없이)                 |
| F10 | 연결 수 표시    | 현재 내 계정의 활성 소켓 수 배지 — 다중 탭 동작을 **눈으로 증명**하는 데모 장치      |
| F11 | 운영 기본       | `/health`, Swagger, Dockerfile, GitHub Actions CI                                    |

### 2-2. 명시적 제외 (S 티어 과설계 방지)

- ❌ **Redis 어댑터 / 다중 인스턴스 스케일링** — 단일 인스턴스 전제. "왜 지금은 필요 없고 언제 필요해지는지"만 README·STUDY에 서술.
- ❌ **알림 종류 다양화** — 좋아요·팔로우·멘션 없음. `COMMENT` 하나. (그래서 `type` 컬럼은 남기되 값은 1종)
- ❌ **알림 상태 확장** — 보관/삭제/음소거 없음. `isRead` boolean 하나.
- ❌ 게시글·댓글 **수정/삭제**, 카테고리, 태그, 좋아요, 검색, 파일 업로드
- ❌ Refresh Token 회전 (newGym에서 이미 커버) · OAuth · RBAC
- ❌ 타이핑 인디케이터·온라인 유저 목록·채팅 (알림함 범위 밖)
- ❌ socket.io `connectionStateRecovery` — 3장 성공기준 SC-2 참고. 의도적으로 **안 쓴다**.

---

## 3. 유저 스토리

### 방문자 / 인증

- **US-1** 면접관으로서, 회원가입 없이 **"회원가입 없이 둘러보기"** 버튼 한 번으로 데이터가 채워진 화면에 들어가고 싶다.
- **US-2** 사용자로서, 이메일·닉네임·비밀번호로 가입하고 로그인해 내 계정으로 활동하고 싶다.

### 게시판

- **US-3** 사용자로서, 최신순 게시글 목록에서 제목·작성자·댓글 수를 보고 싶다.
- **US-4** 사용자로서, 글을 작성하고 다른 사람의 글에 댓글을 달고 싶다.

### 알림 (핵심)

- **US-5** 글쓴이로서, 내 글에 댓글이 달리면 **새로고침 없이 즉시** 알림 뱃지가 올라가는 걸 보고 싶다.
- **US-6** 글쓴이로서, **내가 내 글에 단 댓글로는 알림을 받고 싶지 않다.**
- **US-7** 사용자로서, 접속하지 않은 동안 쌓인 알림을 **다음 로그인 때 빠짐없이** 보고 싶다.
- **US-8** 사용자로서, 잠깐 네트워크가 끊겼다 돌아왔을 때 **그 사이에 온 알림이 사라지지 않기를** 원한다.
- **US-9** 사용자로서, PC와 노트북 두 곳에 로그인해 있으면 **양쪽 모두** 알림을 받고 싶다.
- **US-10** 사용자로서, 한 탭에서 알림을 읽으면 **다른 탭의 뱃지도 같이 줄어들기를** 원한다.
- **US-11** 사용자로서, 알림을 클릭하면 그 댓글이 달린 게시글로 이동하고 싶다.

---

## 4. 성공 기준 (DoD)

### 4-1. 기능 성공 기준 — 수동 QA 시나리오로 검증

| ID       | 기준                                                                                                                                                              | 검증 방법                                                                                                        |
| -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| **SC-1** | **실시간 전달**: 유저 B가 유저 A의 글에 댓글 → A 화면이 **1초 이내** 갱신(새로고침 없음)                                                                          | 브라우저 2개(A/B) 나란히 두고 육안 확인 + 데모 GIF                                                               |
| **SC-2** | **재연결 유실 0건** ⭐: A 접속 중 서버·네트워크를 끊고(DevTools Offline 또는 서버 재시작) 그 사이 B가 댓글 3개 작성 → A 복구 시 **3건 모두** 뱃지/목록에 나타난다 | DevTools Network를 Offline→Online 토글. 소켓 emit은 전부 유실된 상태에서 REST 재동기화만으로 3건이 채워져야 통과 |
| **SC-3** | **중복 0건** ⭐: SC-2 직후 알림 목록에 **같은 알림이 두 번 보이지 않는다**(소켓 수신분 + REST 재조회분 병합)                                                      | 알림 id 기준 육안 확인. 이게 깨지면 SC-2 해법이 틀린 것                                                          |
| **SC-4** | **다중 탭 브로드캐스트** ⭐: A가 탭 3개로 로그인 → B가 댓글 1개 → **3개 탭 모두** 뱃지가 오른다. 연결 수 배지에 `3`이 표시된다                                    | 탭 3개 육안 확인 + 데모 GIF                                                                                      |
| **SC-5** | **읽음 동기화**: 탭1에서 "모두 읽음" → 탭2·탭3 뱃지도 0이 된다                                                                                                    | 탭 3개 육안 확인                                                                                                 |
| **SC-6** | **자기 댓글 알림 없음**: 자기 글에 자기가 댓글 → 알림 0건, 뱃지 변화 없음                                                                                         | 단위 테스트 + 육안                                                                                               |
| **SC-7** | **소켓 인증**: 토큰 없음/위조/만료 토큰으로 소켓 연결 시도 → `connect_error`로 거부되고, 클라이언트가 **무한 재연결 루프를 돌지 않는다**                          | 콘솔에서 토큰 조작 후 Network 탭의 폴링 재시도 횟수 확인                                                         |
| **SC-8** | **트랜잭션 원자성**: 알림 저장이 실패하면 댓글도 저장되지 않는다(그 반대도). 커밋 성공 이후에만 소켓 emit이 나간다                                                | 코드 리뷰 + 단위 테스트(알림 저장 강제 실패 주입 시 댓글 롤백)                                                   |
| **SC-9** | **데모 계정**: `admin`/`admin` 원클릭 로그인 성공. 로그인 화면에 **"회원가입 없이 둘러보기"** 버튼 노출, 안내문 **"회원가입 없이 체험해 볼 수 있습니다."**        | 육안. 단 비밀번호는 bcrypt 정상 비교를 통과해야 하고, 인증 우회 엔드포인트는 존재하지 않아야 함                  |

### 4-2. 품질 게이트 (S 티어 CLAUDE.md 표 기준)

- ✅ Passport-JWT 인증 / ✅ @nestjs/swagger 문서 / ✅ README + mermaid 아키텍처 다이어그램
- ✅ 핵심만 단위 테스트: **알림 생성 규칙(SC-6/SC-8)**, **소켓 인증(SC-7)**, **미읽음 조회/카운트** 3개 스위트 (Jest + supertest)
- ✅ GitHub Actions CI (lint → build → test) / ✅ 백엔드 Dockerfile
- ✅ TypeORM 마이그레이션 파일 + 알림 복합 인덱스 (DATA-MODEL.md 참고)
- ⛔ Testcontainers 통합 테스트 — S 티어 면제(표에서 `—`). 단위 테스트는 리포지토리 목으로 DB 없이 돈다.
- 🚩 라이브 배포: 프론트 Vercel. **백엔드는 Render(Docker) 배포 시 WebSocket 데모를 링크 하나로 보여줄 수 있어 어필력이 크게 오르므로 후보로 남긴다.** Render 무료 티어는 유휴 시 스핀다운되어 첫 접속에 콜드스타트가 발생하는데, 이는 오히려 SC-2(재연결 재동기화)가 실제로 필요한 상황을 라이브에서 재현해준다. **최종 배포 여부는 사람 승인 게이트에서 결정한다.**

### 4-3. "완료"의 정의

위 SC-1 ~ SC-9가 전부 통과하고, **SC-2 / SC-3 / SC-4를 각각 GIF로 README에 박아 넣은 시점**에 완료.
GIF 3개가 없으면 이 프로젝트의 차별화 포인트는 면접관에게 존재하지 않는 것과 같다.
